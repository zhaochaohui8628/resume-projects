"""切分与回扩的长度口径——**唯一事实源**。

三个数不能各写各的：语料层切子块、索引层建单元、检索层回扩父块必须用同一套口径，
否则会出现"索引按 700 切、回扩按 2000 算"这种对不上的情况。

## ⚠️ 切分触发阈值 ≠ 子块尺寸（第五轮修正的核心口径）

- `MAX_PARENT_LEN`（1200）是**"是否切分"的判据**：条款 ≤ 1200 字 → **整条直用**，
  不做滑窗、不做父子块（合理送入 LLM 范围内，语义完整）；
- `CHUNK_SIZE`（700）只是**过长条款的子块目标长度**：仅 >1200 字的条款才按 700/100 切。
- 旧实现把 700 同时当阈值用 → 700~1200 字条款被无谓切成多块，整条语义被肢解。
  修正后实测 MRR `0.878→0.890`（+1.1pt）。

## MAX_PARENT_LEN 怎么定的（2026-09-11，按 13661 父块的长度分布）

实测分布：p50=82 / p90=314 / p95=476 / p99=1301 / max=96986（字符）

| 阈值 | 触发兜底的父块 | 占比 | 这些父块占全文比例 |
|---|---|---|---|
| 700 | 362 | 2.6% | 37.1% |
| 1000 | 202 | 1.5% | 31.9% |
| **1200** | **153** | **1.1%** | **29.9%** |
| 1500 | 111 | 0.8% | 27.7% |
| 2000 | 72 | 0.5% | 25.1% |

取 **1200 = 约 p99**：98.9% 的条款整条直用（语义完整），只有 1.1% 的"过长条款"
（附录大表格、多级编号长条）才切分并走"命中子块周边窗口"回扩。再往上放宽到 2000
只多救回 81 个父块，却把送进 LLM 的单块长度抬到近 3 倍——收益不成比例。
"""
from __future__ import annotations

CHUNK_SIZE = 700        # 子块 / 定长块的目标长度（仅对"过长条款"生效）
CHUNK_OVERLAP = 100     # 相邻子块重叠
MAX_PARENT_LEN = 1200   # ①切分阈值：> 本值的条款才切分；②父块最长限制：超过只回扩命中子块周边窗口（窗口总长 = 本值）
