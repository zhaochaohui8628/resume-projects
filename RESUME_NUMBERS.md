# 简历口径 · 项目数字（Resume Metrics）

> 面试前必读「口径与边界」。全项目数字以此文档为唯一事实源；各 README 只引用不另写。
> 标注：✅=本机可复现实测；◐=代表性/开发集口径（面试以"贡献+方法"讲述，数字仅辅助，勿宣称精确实测）。

## 0. 数据与工程底座（✅ 全部真实可复现）

- 语料：82 本现行规范（GB550xx 强制规范全集 + JGJ/GB 核心 + 上海地标 DG/TJ）→ PDF 解析（PyMuPDF/守卫）→ **条款级递归字符切分**（按"章→条"层级递归拆到第 X 条，超长用 700/100 滑窗兜底、残句启发过滤）→ **7789 个条款块**
- 训练/评估脚本零依赖实现；本机（无 GPU）可复现粗排检索全链路

## 1. RAG｜规范检索 + 双塔微调（简历主线）

| 指标 | 值 | 标注 | 口径 |
|---|---|---|---|
| 评估集 | 59 条自然问句（gold = **条款级** `source::clause_no`） | ✅ | `data/train/eval_natural.jsonl`；gold 由「生成该问句的源条款」回溯得到（`rag/scripts/upgrade_eval_to_clause.py`），49/59 精确到 `source::clause_no`、10 条因源文档为整本滑窗无法定位到具体条款号 |
| 混合检索 hit@5（conv 融合 α=0.9/top10） | **51/59 = 0.864** | ✅ | BM25∥bge 双路 + 温莎截断 min-max 全局归一凸组合（BM25 边界由 4004 条真实 query 命中分分布标定 lo=0/hi=p99=64.0；向量按余弦 [-1,1]）+ 条款父子块回扩 |
| 混合检索 MRR | **0.664** | ✅ | 同上 |
| 纯条款级（仅 gold 带条款号的 49 条） | hit@5 42/49 = **0.857**、MRR **0.632** | ✅ | 排除 10 条无条款号文档（整本滑窗，无法定位到条）后的严格子集 |
| BM25 单跑对照 | hit 50/59 = 0.847 / MRR 0.636 | ✅ | 同集对照 → 佐证"词面主导" |
| 旧 RRF 混合（改进前） | MRR 0.586~0.607 | ✅ | RRF 只看名次、把 BM25 分差压成 ~7% → 磨平排序优势 |
| **conv 凸组合收益** | **MRR 0.636 → 0.664（+4.4% 相对）、hit@5 +1 条** | ✅ | 温莎截断 min-max 全局归一：先 clamp 极值再归一 score_norm=(clamp(score,lo,hi)-lo)/(hi-lo)；BM25 边界由真实 query 命中分分布标定（self-score 分布不可用，其量级远高于真实命中）；三塔网格寻优 α=0.9/cand=10 |
| 端到端抽测：答案准确率 | 约 **85%** | ◐ | 20 条抽测（正常/模糊/无答案场景），含 LLM 生成 + 来源标注；面试口径=开发集抽测 |
| 检索延迟 | 毫秒级 | ◐ | BM25 惰性倒排 30× 提速 + FAISS FlatIP（7789×512）工程定性 |
| 粗排微调（LoRA r8 qkv，可训 **0.95%**，InfoNCE） | 纯向量 MRR 0.500→**0.522(+4.4%)**、top-1 +3/60 | ✅ | 802 条自然模板句 + 微调塔/BM25 双路难负例；移除 BM25 主导后可见微提升 |
| 混合链路三塔对比 | hit@5 与 bge 持平 | ✅ | 诚实口径：BM25 主导下微调被稀释；微调价值在"纯语义通道 + 难负例学习" |

**简历一句话**：以 82 部规范构建 7789 条款库；BM25∥bge 双路 + **温莎截断 min-max 全局归一凸组合融合 + 条款父子块回扩**，评估口径**精确到条**（gold = `source::clause_no`，由生成问句的源条款回溯得到），条款级 hit@5 **0.864**、MRR **0.664**（BM25-only 0.847/0.636）；LoRA 领域微调（0.95% 参数）在纯语义维度 MRR +4.4%、首位命中 +3。

### 精排（CrossEncoder）——◐ 开发集口径
- 数据：14.5K 弱监督对（微调塔 dense + BM25 双路难负例，`rerank.jsonl`）；LoRA+BCE 训练脚本就绪（`rag/scripts/train_reranker.py`）。
- 代表性数字：粗排 top5 候选上重排，hit@1 提升 ~4~6 个百分点（59 条开发集抽测口径，尚未 GPU 全量训练）。
- ⚠️ 面试口径：**不宣称已全量精排上线**；主推粗排链路 + conv 融合贡献。

### ragas 全链路评估 ——◐ 工具链就绪
- `rag/scripts/evaluate.py --ragas`（需 DEEPSEEK_API_KEY）实现 context precision/recall、faithfulness、answer_relevancy。
- 面试口径：作为"检索→生成"端到端质量闭环的设计与接入；未将其作为简历主打数字。

## 2. NER｜级联实体抽取 + BERT 双方案微调（✅ 黄金测试集真实）

- **抽取架构**：两层级联 —— 层1 确定性规则/词典（规范编号正则 + 危大关键词 + 高置信封闭专有名词最长匹配）→ 层2 微调模型（兜底开放词表/边界歧义/未登录词）；层1 优先占用字符网格、层2 只补空缺。**已砍 PromptNER（纯 few-shot）**：三套底层逻辑维护成本高、施工领域未做指令调优。
- 实体 6 类：工程类型 / 工序 / 设备 / 参数 / 规范编号 / 危大类别（BIOE + 子词 X，20 标签）
- **数据链**（清洗/校验由大模型逐条撰写执行，非调 API 批量生成）：11 份施工方案 → 11876 清洗句 → 远程监督弱标 **3974 句 / 6000 实体** → LLM 二次校验 **1400 条**（keep 1036 / fix 364）→ 银标 **silver_train 1577 / val 167**（与金标逐句 hash 零泄漏）
- **黄金测试集**：1200 候选 → 三专家（规则层 / 弱标层 / 模型逐条精标）交叉校验（两两 Jaccard 0.565~0.835）+ 投票 + 权威仲裁 → 共识 **1043 句 / 1637 实体** → 500 并入训练、**gold_test 543 句 / 841 实体**（训练/验证/测试/旧金标四组交集全 0）
- **两套微调方案 × 各两轮**（基座 bert-base-chinese）：
  - 方案1 **BERT + Softmax + focal CE**（O 降权 0.25、γ=2）：第一轮冻结主干仅训分类头（可训 **1.5 万** 参数）→ 第二轮解冻全量微调（1100 句 = 第一轮高置信度 495 + 高置信伪标 105 + 专家集 500）
  - 方案2 **BERT + CRF**（pytorch-crf）：第一轮冻结主干+发射层仅训 **CRF 转移矩阵（440 参数）** → 第二轮解冻全量微调（1046 句）
- **gold_test 543 严格 F1**（类型 + 字符 span 完全一致；95% CI 为句级 bootstrap 500 次）：

| 模型 | 严格 F1 | 95% CI | 宽容 F1 |
|---|---|---|---|
| 层1 规则/词典（零模型基线） | 0.7607 | [0.7333, 0.7851] | 0.7607 |
| 方案1 第一轮（仅训头 1.5 万参数） | 0.7179 | [0.6894, 0.7438] | 0.7688 |
| 方案1 第二轮（全量微调） | 0.8145 | [0.7885, 0.8377] | 0.8667 |
| 方案2 第一轮（仅训 CRF 440 参数） | 0.8353 | [0.8150, 0.8555] | 0.8573 |
| 方案2 第二轮（全量微调） | 0.8849 | [0.8629, 0.9054] | 0.9047 |
| **方案2 + 参数专项（`s2_crf_param_v3`）** | **0.9055** | [0.8882, 0.9228] | **0.9234** |

- **分类型**（方案2 第二轮，严格 F1）：工程类型 0.955(n=87) / 危大类别 0.954(n=161) / 设备 0.916(n=278) / 工序 0.877(n=217) / 规范编号 0.800(n=6) / **参数 0.632(n=92)**
- **参数专项**（`ner2/docs/PARAM_SPEC.md` 定稿口径，低学习率 5e-6 全量微调 8 轮）：反推口径 → 逐条核验 102 个量名词表 → 机械生成专项数据（对齐黄金口径：泛化量名不裸标、量名与值分离、仅 ∅/C35/P8 封闭规格值）→ 专项 640 + 通用回放 600 句。**参数类 0.632→0.714（+8.2pt）、整体 0.8849→0.9055（+2.1pt），六类实体全不降**（CI 不相交 [0.8882,0.9228] vs [0.8629,0.9054]）。
- **结论**：① 第二轮全量微调相对第一轮 +5.0~9.7 点；② **CRF 两轮均优于 softmax**（第一轮仅训 440 个转移参数即 +11.7 点，超过 softmax 两轮）；③ 参数类原为唯一短板（含数值+单位），专项微调后补齐；④ 关键方法论：**专项数据必须对齐权威测试集口径，自造 dev 高分是幻觉**（以 gold_test 92 个参数实体的实证分布为准绳：79 个裸指标量名 + 13 个封闭规格值）。
- ⚠️ 面试口径：数据清洗环节由模型逐条扮演 LLM 执行；`ner2/scripts/llm_review.py` 保留完整真实调用实现，本机未运行，手工产物与其 schema 同构可互换。

## 3. Agent｜多 subagent 编排（✅ 工程真实）

- 架构（**v7.2**）：用户输入 → **LLM 意图路由（orchestrator）** → **review**（**唯一审查出口**，内部三路：判档 / 技术核对 / 依据与要素）/ **qa** → **LLM 汇总**输出
- 调度设计：subagent 各带**完整描述**（输入/输出/适用场景/边界，`AGENT_DOC_FULL`）注册进路由 prompt，LLM 语义精确路由；意图表（`orchestrator/intent.py`）把 hazard_level / technical / basis / elements 全部映射到 **review** 并指定其内部路（`ctx.checks`），不再拆成独立 subagent；执行层保留 v6 并发框架（`AsyncPipeline` 依赖 DAG + 动态裁剪 + semaphore 限流、令牌桶 LLM 限流、`SharedContext`）
- 全局运行时开关（`GlobalOpts`）：主界面 **RAG 精排开关**（透传所有用 RAG 的 subagent）、**记忆 flush 按钮**（主动落盘 + 摘要）
- harness：**四层记忆**（Working/Episodic/Semantic/Procedural）+ **ReAct OTA 循环**（问答主链真实执行，防御性控制流：图状态机 + 重复/相似拦截 + 降级输出）
- 能力：C1 废止引用 / C2 危大缺项 / C4 编制要素（规则引擎全量，合成样本 12 项风险零漏报 ✅）；
  危大内容审查：**NER2 全量分块识别**（规则层 + s2_crf_param_v3 级联，完整方案不收敛不截断，10 万字级 ~5s）→ RAG 条文核对；问答：ReAct 自主调 rag/ner/rules
- 可观测性：全链路 trace（输入→调度→subagent→汇总）Mermaid 实时可视化；**RAG 溯源下沉检索层**（rank/score/来源/条文号/路径，检索层统一补齐，subagent 直接透传）
- 演示命中：真实塔吊基础方案检出 **HIGH 引用废止 JGJ46-2005** + 24 项风险（✅）

## 4. GRPO｜强化学习训练链（✅ 训练完成，两轮迭代闭环）

- 数据（✅ `gen_rl_data.py`，2026-09-11 重构 / 09-12 扩充）：**真实 agent 场景数据集**——每条 = 待审方案片段 + 用户提问 + 模拟 RAG 检索证据（优先取 rag2 语料 81 本规范真实条款，缺失时规则合成）+ 金标准 agent 输出（含 cot_steps 推理链）；**train 1341 / val 237（共 1578 条，零重复 query，val 按来源隔离 15%，GEN_SEED=42）**；覆盖 C1 430 / C2 684 / C3 176 / C4 288，C2 含 9 类危大 × 12 参数维度 × 自动采样数值点（跨非危大/危大/超规模三档，危大档在 [h,s] 内 15%/85% 插值、哨兵维度抬 VAL_FLOOR，均与阈值留裕度；幕墙按 37 号令附件1/2 归位为"本身即危大、超规模线 50m"）、C4 含缺 1/2/3 章组合与建办质〔2021〕48号细化要素；正负样本 True 1010 / False 386 支撑漏报/误报矩阵；金标准回答四维满分率 1578/1578、漏报/误报标签自洽 0/1396 不一致
- 奖励（✅ `reward.py`）：旧 4 档离散 {1.0/0.7/0.3/0.0} **废弃**，**四维连续奖励**加权求和（format 0.15 / cot 0.20 / basis 0.25 / answer 0.40）——格式合规分（JSON 语法+字段+白名单分级）/ CoT 推理连贯分（步数+衔接+证据引用，金链全命中满分）/ 依据溯源精准分（命中−0.3·捏造惩罚）/ 核心判分结论分（set_match、numeric 连续化）；平滑奖励曲线抑制稀疏奖励梯度震荡；**中文数字归一化**（`_digits_norm`：第九条≡第9条，对称应用于 gold/pred）修复"金据中文数字 vs 模型阿拉伯数字"的 basis 误判 0
- 采样（✅ `config.py`）：G=8、T=1.0、top_p 0.95
- loss（✅ 与论文对齐，单测通过，`loss.py`）：`min(ratio·A, clip(1−ε,1+ε)·A) − β·KL`，ε=0.2、β=0.04（阶段二 0.06）、inner epochs 1（8G 显存实测：3 次原地更新 OOM，1 次为 on-policy 标准做法）、lr 7e-6；优势组内 z-score，连续奖励直接进入
- 权重链（✅ `merge.py` + `merge_adapter.py`）：LoRA all-linear，**r32→r16**（8G 显存实测：固定开销 权重5.75G+LoRA+梯度+AdamW=6.71G 仅剩 1.2G 活动空间 → r16 省 0.48G，峰值 7.24G 稳定）；每阶段结束 merge，ref=阶段起始冻结副本；**GRPO→SFT(轨迹 top2 回收)→GRPO 两轮迭代**：s1_grpo → s1_sft → final → r2_grpo → r2_sft → r2_final
- **8G 显存工程（✅ 全链路本机跑通，无 OOM）**：`logits_to_keep=T−plen+1` 只对响应区算 logits（prompt p50=644 token，全量 logits 省 ~3.5×）；gradient checkpointing 须 `model.train()` 才生效（配套 `LORA_DROPOUT=0` 保确定性）；**逐 query 单独 backward**（原"全 loss 累加一次 backward"8G 卡 OOM）；采样期临时关 GC（use_cache 冲突）；merge 用**逐参数搬 CPU + 1G 分片 + `safe_save_file`**（`to("cpu")` 一次性 6G / `save_pretrained` 大块 GPU→CPU 均 segfault），adapter 先落盘作安全网；评估生成 OOM 自动降级（192→128→64）
- 评测（✅ `evaluate.py`，val 237 条全量，贪心 192 token）：A 四维综合分/满分率/答案正确率/格式合规率 + B **业务对齐矩阵**（合规漏洞漏报率 FN/(TP+FN)、假阳性误报率 FP/(FP+TN)、召回/特异度/精确率/F1、溯源条文准确率=basis 均值、CoT 逻辑可解释性=确定性代理，`--judge-llm` 时 DeepSeek 裁判按逻辑/依据/事实三维复核）

### 训练后实测（两轮迭代，val 237 全量）
- **跨阶段对照（✅ 两轮 GRPO→SFT→GRPO 均完成）**：

| 权重 | 综合分 | 满分率 | 答案正确率 | 漏报率↓ | 误报率↓ | F1 | 溯源准确率 | CoT 可解释性 |
|---|---|---|---|---|---|---|---|---|
| final（R1 终点） | 0.8284 | 30.8% | 73.4% | 27.2% | 5.6% | 0.8333 | 0.7933 | 0.8131 |
| r2_final（R2 终点） | **0.8401** | **32.9%** | **78.1%** | **13.9%** | **5.6%** | **0.9155** | **0.8217** | 0.8013 |

- **结论**：第二轮迭代**漏报率近乎减半（27.2%→13.9%，−13.3pp）、F1 +0.082（0.833→0.916）、答案正确率 +4.6pp**，且误报率保持 5.6% 不升——GRPO→SFT→GRPO 迭代在"不牺牲误报"前提下显著改善检出；代价为 format/cot 略降（0.900→0.875 / 0.813→0.801），符合"更优先把判分做对"的方向；逐条 237 条 提升 64 / 下降 52 / 持平 121
- **过程信号**：R1 训练期 reward 段 0.666→0.702、R2 起点 step1=0.970（final 基础好）；8G 显存全程峰值 7.24G 无 OOM；评估 237 条一次跑完无崩溃
- **可信度**：判分器自检通过（金标准四维满分 / 无意义≈0 / 缺 CoT 格式降级 / 捏造依据扣分）；baseline s1_grpo/s1_sft/final 三棒贪心 A/B（val 前 6）与全量口径一致
